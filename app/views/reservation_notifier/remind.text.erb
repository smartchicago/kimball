Hi <% @reservations.first.person.first_name %>,

<%= @reservations.size %> interview<%= @reservation.size > 1 ? 's have' : ' has' %> been booked for you today.

<% @reservations.each do |r| %>
<%= r.description %> on <%=r.to_weekday_and_time %> for <%= r.duration / 60 %>minutes \n with <%= r.user.name %> tel: <%= r.user.phone_number %>
To cancel, confirm, or reschedule with the person, click the link below:

Confirm:    https://<%= ENV['PRODUCTION_SERVER'] %>/v2/reservation/<%= r.id %>/confirm/?token=<%=r.person.token%>

Cancel:     https://<%= ENV['PRODUCTION_SERVER'] %>/v2/reservation/<%= r.id %>/cancel/?token=<%= r.person.token %>

Reschedule: https://<%=ENV['PRODUCTION_SERVER']%>/v2/reservation/<%= r.id %>/reschedule/?token=<%= r.person.token %>

<% end %>

You can always check your schedule here: https://<%=ENV['PRODUCTION_SERVER']%>/calendar/?token=<%= @reservations.first.person.token %>


Thanks!,
<%= ENV['TEAM_NAME'] %>

