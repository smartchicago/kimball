<div class="row">
  <div class="span6">
    <%= form_for(@submission) do |f| %>
      <fieldset>
        <legend>Submission</legend>        
        <% if @submission.errors.any? %>
          <div id="error_explanation" class="alert alert-error">
            <ul>
            <% @submission.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <%= f.label :person_id %>
        <%= f.text_field :person_id, class: 'span6' %>

        <%= f.label :entry_id %>
        <%= f.text_field :entry_id, class: 'span6' %>

        <%= f.label :form_structure %>
        <%= f.text_area :form_structure, class: 'span6' %>

        <%= f.label :field_structure %>
        <%= f.text_area :field_structure, class: 'span6' %>

        <%= f.label :raw_content %>
        <%= f.text_area :raw_content, class: 'span6' %>

        <div class="form-actions">
          <%= f.submit 'Save', class: "btn btn-primary" %>
          <%= link_to 'Cancel', :back, class: "btn" %>
        </div>
        
      </fieldset>
    <% end %>    
  </div>
  <div class="span6">
    <p class="well">Programs are the societal issue that individual applications are attempting to address. "Safe passage to school" and "affordable broadband" are examples of Programs.</p>
  </div>
</div>
