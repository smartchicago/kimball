Hello <%= @person.first_name %>,
you've been invited to a <%= @event.duration / 60 %> minute interview on <%=  @event.event_invitation.date %> between <%= @event.event_invitation.to_time_and_weekday %>.

<%= @event.description %>

<%= link_to 'Please click to setup a time for your interview',
         {controller: "calendar", action: "show", token: @person.token} %>
